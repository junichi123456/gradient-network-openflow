# 作業方針

## 意思決定の進め方

判断が必要になった場合、ユーザーに文章での回答を求めない。**必ず選択肢の形で提示する**（`AskUserQuestion` を使用し、クリックのみで決定できる状態にする）。

- 推奨案を第1選択肢に置き、ラベル末尾に「(推奨)」を付す
- 各選択肢には、それを選んだ場合に何が起きるかを明記する
- 「現状維持」「後で決める」も、該当する場合は選択肢として用意する
- 複数の論点がある場合はまとめて提示し、往復を最小化する
- 慣例的な既定値で足りる些細な判断は、質問せずに実行し、結果を報告する

## 対象ドキュメント

- `minecraft_server_spec.md` — Minecraft マルチサーバーの設計仕様書。節番号は本文中で相互参照されるため、節の追加・削除時は参照（§n 表記）の整合を確認する
