# 60人同時接続の負荷見積り

`minecraft_server_spec.md` の設計で **60人が同時にログインしても遅延とロールバックを起こさない**ために必要な条件を、負荷の内訳から導く。

---

## 0. 結論

| 項目 | 必要な水準 |
|---|---|
| サーバーソフト | **Folia**（リージョン並列） |
| CPU | **8コア16スレッド以上・ブースト5GHz級**。単コア性能とコア数の両方が要る |
| メモリ | **実装64GB**（最低32GB）。主サーバー16GB＋レイド／戦争4GB＋残りをページキャッシュへ |
| ディスク | **NVMe SSD 1TB**、書き込み耐久の高いもの。バックアップ用に別ドライブ |
| 回線 | 上り **30Mbps以上**。ポート開放できること（CGNAT不可） |
| 電源 | **UPS 必須**（ロールバック対策の本命） |

> **中古のサーバー機（Xeon 等）は Minecraft では不利。** コア数は多いがクロックが低く、リージョン単位では単スレッド性能がそのまま効く。ECC と IPMI は利点だが、**高クロックのデスクトップCPUのほうが体感が良い**。

---

## 1. この設計で負荷になるもの

### 1.1 次元の数

| 次元 | 寸法 | 常時ロード |
|---|---|---|
| 主世界 | 101×101ch | 部分 |
| Hub | 12×12ch（144チャンク） | **常時** |
| ネザー | 100×100ch | 部分 |
| エンド | 101×101ch | 部分 |
| 資源採掘 | 100×100ch | 部分 |
| レイド | 開催時のみ | 隔週 |
| 戦争 | 別インスタンス | 予約起動 |

**Paper では全次元を1本のメインスレッドで tick する。** 次元が5つある本設計では、60人が分散するほど1本のスレッドに仕事が集まる。ここが Folia を前提にする最大の理由である。

### 1.2 Hub が最大の弱点

Folia は**世界を region に分割し、region ごとに別スレッド**で処理する。分散していれば効くが、**1か所に集まると効かない**。

本設計の Hub は 12×12ch と狭く、全員が1日に複数回訪れる（§13）。**60人が同時に Hub にいると、その region が1スレッドに戻る。**

| 対策 | 内容 |
|---|---|
| 丁目を離す | 一丁目〜四丁目を**十分に離して配置**し、別 region に落ちるようにする。現行の 192×192 では1〜2 region に収まってしまう |
| 滞在時間を削る | 受取窓口を各丁目に分散する設計（§13.5）は既にこの方向。取引だけなら広場から32ブロックで完結する動線も同様 |
| 入場制限 | 最終手段。Hub の同時滞在数に上限を設ける |

> **Hub の区画割りは、見た目ではなく region 分割の都合で決めるべきである。** §13.2 の寸法は再検討の対象になる。

### 1.3 エンティティ

**敵対モブの湧き1.5倍（§2）が常時負荷の中心。** モブは per-player で湧き枠が配分されるため、人数に比例して増える。

| 項目 | 見積り |
|---|---|
| 敵対モブ | 60人 × 湧き枠 × 1.5 → **数百〜2,000体規模** |
| 常設エンティティ（Hub） | 400体（§13.7 の上限） |
| レイド個体 | 表示19 ＋ 判定17 ＝ 36体（隔週・**1枠12名上限**・同じ開催日に6枠）。位置の更新が「実体数 × 20Hz × 観戦者数」で増えるため、参加人数を12名に制限している（§12.3） |
| 額縁・防具立てなど | プレイヤー製。国家の建築次第で増える |

湧き1.5倍は経済設計の柱（§2）なので減らせない。**simulation-distance を絞ることで湧き範囲を抑えるのが現実的**である。

### 1.4 チャンク

| 設定 | 1人あたり | 60人（重複なしの最悪） |
|---|---|---|
| view-distance 10 | 441チャンク | 26,460 |
| simulation-distance 6 | 169チャンク | 10,140 |

**view と simulation を分けるのが要点。** 見える範囲（view）は広くても軽いが、動く範囲（simulation）は重い。simulation-distance 6 は湧き量にも効く。

### 1.5 自作プラグイン

| 処理 | 頻度 | 重さ |
|---|---|---|
| 有効活動時間（§2.1） | 移動イベントごと | **軽い**。pitch は真偽で保持する設計にした |
| 領土の解決（§4.11） | チャンクを跨いだ時のみ | 軽い。ハッシュで O(1) |
| 境界の表示（§4.11） | 棒の所持者のみ | 中。所持者数に比例 |
| GUI市場の照合（§14） | 注文のたび | 軽い |
| 国庫・外交の定期処理 | 日次・30日ごと | 軽い |
| レイドの変換送信（§12.6） | 隔週・毎秒4,000件上限 | 中。上限を設計に組み込み済み |

**プラグイン起因の常時負荷は小さい。** 重いのはバニラ側（モブとチャンク）である。

### 1.6 次元の再生成

| 対象 | 頻度 | 影響 |
|---|---|---|
| 資源採掘ワールド | 週2回（火・金 03:00） | 100×100ch の削除と再生成。**ディスクI/Oの山** |
| ネザー | 毎月1日 05:00 | 同上 |
| エンド | 毎週土曜 15:00 | 同上 |

深夜・早朝に寄せる設計（§1.1）は正しい。**Chunky による事前生成（§21）を必ず併用する。**

### 1.7 回線

| 項目 | 見積り |
|---|---|
| 1人あたり上り | 150〜250 kbps |
| 60人 | **9〜15 Mbps** |
| レイド時の上乗せ | 毎秒4,000件の変換更新 |
| 推奨 | **上り30Mbps以上** |

---

## 2. ロールバックを防ぐ

「ロールバック」は**サーバーが落ちて直前の保存状態に戻る**ことを指す。原因は4つに絞られる。

| 原因 | 対策 |
|---|---|
| **メモリ不足**（ヒープ枯渇 → GC で停止 → 強制終了） | ヒープを実装メモリの半分以下に抑える。64GB 実装なら 16GB 割り当て |
| **停電・強制終了**（保存中の破損） | **UPS**。これが最も効く |
| **tick の長時間停止**（watchdog による強制終了） | 重い処理を非同期に逃がす。Folia では region ごとに分かれるため影響が局所化する |
| ディスクの書き込み失敗 | NVMe の健全性監視。バックアップを**別ドライブ**に置く |

**保存とバックアップ**

| 項目 | 設定 |
|---|---|
| 自動保存 | 5分ごと |
| バックアップ | 日次で別ドライブへ。世界とプラグインのデータを分けて取る |
| 領土・国庫・活動時間 | ワールドとは別に SQLite で保持し、**ワールドが壊れても制度データは残る**構成にする |

> 最後の項目が重要である。ワールドの破損とデータベースの破損を切り離せば、片方が壊れても復旧できる。

---

## 3. 必要なハードウェア

### 3.1 CPU

Folia は region ごとに1スレッドを使う。したがって **単コア性能とコア数の両方**が効く。

| 候補 | 構成 | 評価 |
|---|---|---|
| **Ryzen 7 9700X**（デスクトップ） | 8C/16T・5.5GHz・L3 32MB | **推奨**。クロックを維持でき、価格も現実的 |
| Ryzen 9 9900X（デスクトップ） | 12C/24T・5.6GHz | 余裕を持たせたい場合 |
| Ryzen 7 8845HS（ミニPC） | 8C/16T・5.1GHz | **60人は上限に近い**。持続負荷でクロックが落ちる |
| Ryzen AI Max+ 395（ミニPC） | 16C/32T | コア数は十分。**メモリ直付けで増設できない**点に注意 |
| 中古 Xeon（サーバー機） | 多コア・低クロック | **不利**。region あたりの処理が遅い |

### 3.2 メモリ

| 用途 | 割り当て |
|---|---|
| 主サーバー（Folia）のヒープ | 16GB |
| レイド／戦争サーバー | 4GB |
| OS | 4GB |
| **ページキャッシュ**（チャンクI/Oの吸収） | 残り全部 |

**実装64GBを推奨する。** 余ったメモリはページキャッシュとして働き、チャンク読み込みのディスクアクセスを減らす。32GB でも動くが、次元の再生成中に余裕がなくなる。

### 3.3 ディスク

- **NVMe SSD 1TB**。週2回の再生成で書き込み量が多いため、**書き込み耐久（TBW）の高いもの**を選ぶ
- バックアップは**別のドライブ**に置く。同じディスクでは意味がない

### 3.4 電源・設置

- **UPS を必ず入れる**。1万円台で足り、ロールバック対策として最も費用対効果が高い
- 24時間運転では冷却と埃が効いてくる。ミニPCは静かだが、**持続負荷でクロックが落ちる**。60人規模ではデスクトップ筐体のほうが安定する

---

## 4. Folia を前提にすることの影響

**プラグインの書き方に制約が入る。** 既存のコードにも修正が必要になる。

| 項目 | 内容 |
|---|---|
| スケジューラ | `runTaskTimer` は使えない。**GlobalRegionScheduler / RegionScheduler / EntityScheduler** に置き換える。`KnightBoss` は書き換えが必要 |
| エンティティ操作 | **所有 region のスレッドからのみ**触れる。他 region のエンティティを直接操作できない |
| 共有状態 | 領土・国庫・活動時間は複数 region スレッドから触られる。**スレッドセーフにする**必要がある |
| 外部プラグイン | Folia 非対応のものがある。**導入前に対応状況を確認する** |

> **コア層は相性が良い。** 数式・判定・照合はいずれも不変オブジェクトを返す純関数として実装してあり、そのまま複数スレッドから呼べる。可変状態を持つのは `ActivityWindow`・`AidLedger`・`OrderBook` の3つで、ここだけ同期を入れれば足りる。

---

## 5. 段階的な進め方

| 段 | 内容 |
|---|---|
| 1 | まず **Paper・少人数**で始める。制度とレイドの検証を優先する |
| 2 | 同時20人前後で `/tps` と tick 時間を計測し、**どこが重いかを実測**する |
| 3 | 60人が見えてきた時点で Folia へ移行する。**移行の判断材料は実測値であり、推測ではない** |
| 4 | Hub の区画割りを region 分割の観点で見直す（§13.2） |

**先に買うべきものは UPS と NVMe である。** CPU は実測を見てからでも遅くない。
