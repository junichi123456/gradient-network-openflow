# 実装の実現性検証

> `minecraft_server_spec.md` の各規定について、既存プラグインで足りる範囲と自作が必要な範囲を切り分けた検証。
> 節番号は仕様書のものを指す。

---

## 0. 結論

| 判定 | 意味 |
|---|---|
| **A** | 既存プラグイン、データパック、バニラ機能で足りる |
| **B** | 自作が必要。API上の実現方法が明確で、難度は低い |
| **C** | 自作が必要。挙動そのものを作る必要がある（**該当なし**） |

| 領域 | 判定 | 根拠 |
|---|---|---|
| 次元ワイプ（§1.1） | A | ワールドの unload → リージョンファイル削除 → 再生成。既存のマルチワールド系プラグインの組み合わせで足りる |
| 除外要素（§1.4） | A | レシピ削除、スポーン抑制、ルートテーブル編集 |
| 保護区（§1.2） | A | ワールドガード系の領域保護 |
| 日次exp上限・倍率（§2） | B | exp変動イベントの捕捉と加算の書き換え |
| **有効活動時間（§2.1）** | **B** | pitch変化の検出。下記1を参照 |
| **『消滅の呪い』の全面付与（§3）** | **B** | 付与経路の網羅が本質。下記2を参照 |
| **領土システム（§4）** | **B（最大工数）** | 数式・ランク・チャンク解放・座標告知。既存の土地保護プラグインでは表現できない |
| 役職・権限・貢献度（§6） | B | データ管理のみ |
| 国庫・外交・統一（§7〜8） | B | データ管理と定期処理 |
| 制裁（§10） | B | 定期徴収とDiscord連携 |
| 戦争競技（§11）| B | サルファーキューブ（26.2）の種類定義をデータパックで6種書く。下記3を参照 |
| Hub と GUI市場（§12〜13） | B | オーダーブック型の板取引は既存の商店系プラグインでは代替できない |
| シュルカーの国家バインド（§14） | B | アイテムのNBTによる所属記録と拾得制限 |
| Mod検出の統計監査（§18.4） | B | 設置レート・移動整合の集計。完全な検証は不可能という前提（仕様側に明記済み） |

**自作が避けられない中核は、領土システム（§4）とGUI市場（§13）の2点である。** 戦争競技（§11）はデータパックの設定に収まり、判定Cの項目は残っていない。 他はほぼ既存資産の組み合わせで足りる。

---

## 1. 有効活動時間の pitch 判定（§2.1）

### 実装案

仕様は「12分の窓内で pitch の総変化量が 0 なら非計上」と定める。総変化量の**値は使われない**（0か否かのみが判定に使われる）ため、実装は累積ではなく**真偽フラグ**で足りる。

```
窓ごとに boolean moved = false
pitch が変化したら moved = true（以降は何もしない）
12分経過時: moved なら12分を計上。いずれの場合もフラグと窓をリセット
```

これは O(1) であり、プレイヤー数に対して線形にしかならない。

### 検出手段の選択

| 手段 | 長所 | 短所 |
|---|---|---|
| 移動イベントの捕捉 | 取りこぼしがない | 発火頻度が高い。ただし `from.pitch != to.pitch` の即時判定のみなら負荷は無視できる |
| 一定間隔のサンプリング（1秒ごと） | 負荷が最小 | 1秒以内に動かして元の値へ正確に戻す操作を取りこぼす |

**推奨は移動イベント**。サンプリングは、視点を動かして正確に元の角度へ戻すマクロを取りこぼす経路が原理的に残る。

### 最初に確認すべきこと

> **移動イベントが「位置は変わらず視点のみ変化した」場合にも発火するか。**

発火するなら実装は数十行で済む。発火しない実装系であれば、20tick ごとのサンプリングに切り替え、判定間隔を短くして取りこぼしを詰める。**この一点だけは、他の実装に先立って確認する価値がある**（仕様の4本柱のうち1本が乗っているため）。

### 付随して必要なもの

- セッション跨ぎでの状態管理（経過時間は繰り越し、フラグはリセット — §2.1 に規定あり）
- 同一IPの丸め込み。接続元アドレスは取得できるが、CGNAT・IPv6・モバイル回線での誤爆は避けられない。**申請制ホワイトリスト（§2.1）が実装上の必須要件である**

---

## 2. 『消滅の呪い』の全面付与（§3）

付与経路の網羅がすべてである。漏れが1経路でもあれば、そこが蓄積の抜け道になる。**開始延期の基準（§19.7）に挙げてあるのはこの理由による。**

| 経路 | 捕捉点 | 備考 |
|---|---|---|
| エンチャントテーブル | エンチャント確定イベント | 結果に呪いを追加 |
| 金床（本＋道具） | 金床の結果生成イベント | **エンチャントが増えた場合のみ**付与。修理・命名では付与しない（§3） |
| 司書村人の本 | 上記の金床経路で捕捉される | 本そのものへの付与は不要 |
| 村人が売る既エンチャント装備 | 取引結果の取得時 | 武器屋・防具屋の完成品 |
| 構造物チェスト | ルートテーブル生成イベント | データパックでの直接編集でも可 |
| モブドロップ | 同上 | |
| 釣り | 同上 | エンチャント本が出る |
| 交易以外のNPC由来（行商人等） | 取引結果の取得時 | 対象があるか要確認 |

### 有利に働く挙動

**砥石は消滅の呪いを除去できない**（バニラの仕様）。したがって「呪いを外して蓄積する」経路は最初から存在しない。仕様の意図と実装が一致している。

### 検証方法

上記8経路それぞれについて、実際に取得して呪いの有無を確認するチェックリストを作り、D-7 に実行する（§19.2 に作業として記載済み）。

---

## 3. 戦争競技のボール物理（§11）

**判定は B である。** サルファーキューブは 26.2 のバニラエンティティであり、§11.5 の値は**データパックで設定できる範囲に収まる**。自前の物理演算は不要。

### 仕様の各要求とバニラ機能の対応

| §11 の要求 | 対応するバニラ機能 |
|---|---|
| 体力無限（§11.2） | ブロックを吸収した状態はほぼ全てのダメージを無効化する。追加の無敵化が不要 |
| 球種ごとの弾性・摩擦（§11.5） | サルファーキューブの種類は `sulfur_cube_archetype` レジストリに属し、`data/<名前空間>/sulfur_cube_archetype` にJSONで定義できる。速度・弾性・地面摩擦・空気抵抗を持つ |
| 水平KB・鉛直KB（§11.5） | 種類定義の `knockback_modifiers`（`horizontal_power` / `vertical_power`）。**既定は 0.33 / 0.06** であり、§11.5 の値はここからの変更値 |
| 球種ローテーション（§11.5） | 吸収させるブロックと種類定義の組み合わせをラウンドごとに切り替える |
| 打撃による弾き飛ばし | バニラ挙動。打撃位置により水平／鉛直の配分と左右の逸れが決まる |

**したがって実装は「種類定義のJSONを6種書き、ラウンドごとに召喚し直す」に還元される。** 表示エンティティによる自前積分の案は不要になったため破棄する。

### ノックバックとダメージ量の関係

**弾き飛ばしの勢いは攻撃のダメージ量に比例するものとして設計する。** §11.3 のロール別シュート力（アタッカー5／フランカー3／ゴーリー1）は、支給装備の攻撃力差としてそのまま表現される。プラグイン側で打撃を検知して速度を加算する処理は不要。

実装後、ロール間のシュート飛距離に想定した差が出ているかを確認する。差が出ない場合は §20「実測後に調整する項目」として扱い、打撃検知による速度加算を追加する。

### 副次的に判明した仕様上の要注意事項

| 事象 | 対応 |
|---|---|
| ハサミで吸収ブロックを取り出せる | 球種の変更と無敵解除ができてしまう。**支給装備から除外**（§11.4 に反映済み） |
| 空のバケツで大型を回収できる | ボールそのものを持ち去れる。**同様に除外**（同） |
| 大型を倒すと2体に分裂する | 吸収状態が維持される限り倒せないため発生しない。上記2点の除外がその前提 |

### 負荷

ボールは1試合に1個。バニラのエンティティ1体であり、負荷の論点は存在しない。

### 出典

情報源はウィキの「サルファーキューブ」および「サルファーキューブの種類のカスタマイズ」の記述に基づく。**本検証の執筆環境からはウィキに直接アクセスできず、検索経由で得た記述に依拠している。** 種類定義のJSONスキーマの全フィールドは、実装前にウィキ本文で確認する。

## 4. 着手順序

| 順 | 対象 | 理由 |
|---|---|---|
| 1 | pitch 判定の発火確認（§2.1） | 数時間で終わり、結果によって実装方式が変わる |
| 2 | 領土システム（§4） | 最大工数。他の多くが依存する |
| 3 | 『消滅の呪い』の経路網羅（§3） | 開始延期の基準に直結 |
| 4 | 次元ワイプと Hub（§1.1・§12） | 既存資産の組み合わせ |
| 5 | GUI市場（§13） | Hub の完成後 |
| 6 | 球種の種類定義6種（§11.5） | データパックのみ。独立して並行可能 |
| 7 | 外交・統一・制裁（§8・§10） | rank5 以降にしか使われないため、開始後の実装でも間に合う |

**§8 と §10 は開始時点で未実装でも運営が成立する。** 同盟の解禁は双方 rank5、制裁は共和制（rank15）が前提であり、最短でも開始から数日〜2週間の猶予がある（§19.5 の到達日程）。

---

## 5. 仕様書側に反映すべき事項

| 事項 | 内容 |
|---|---|
| 反映済み | 球種の値の出所（§11.5）、体力無限の実現方法（§11.2）、ハサミ・バケツの除外（§11.4）はいずれも仕様書へ反映した |
| pitch 判定の実装注記（§2.1） | 総変化量の値は使われず「変化したか否か」のみが判定に用いられる旨を注記する |
| 同一IP判定の限界（§2.1） | CGNAT・モバイル回線での誤爆が原理的に避けられないため、ホワイトリスト申請が必須の運用要件であることを明記する |

---

## 6. レイド個体の実装（§12）

レイド個体は「攻撃モーションとギミックの双方が新規」であることを要件とする（§12.2）。バニラのモブに属性とエンチャントを付けただけのものは要件を満たさない。ここが、これまでの判定（A〜C）とは性質の異なる制約になる。

### 実現方式の比較

**方式A を採用した。** 以下は選定の記録である。

| 方式 | クライアント側の要求 | モーションの自由度 | 主なコスト |
|---|---|---|---|
| **A. リソースパック ＋ 表示エンティティ**（推奨） | サーバーリソースパックの自動適用のみ。**modは不要** | 高い。骨格を分割して個別に動かせる | 通信量とサーバー側のアニメーション実装 |
| B. クライアントmodの配布 | **全参加者にmodのインストールを要求** | 最も高い | 参加障壁、バージョン追随、§20 の許容リストとの整合 |
| C. バニラモブの拡張 | なし | **低い。要件を満たさない** | — |

### 方式A の仕組み

- **見た目**: リソースパックにカスタムモデルを入れ、`item_display` エンティティで表示する。個体を頭・胴・腕・武器などの部位に分け、部位ごとに1エンティティを割り当てる
- **モーション**: 各部位の変換行列（位置・回転・拡縮）をサーバーから更新し、補間時間を指定してクライアント側で滑らかに繋ぐ。**既存モブの動作を一切使わない**ため、要件を満たす
- **当たり判定**: `interaction` エンティティを重ねて被弾を受ける。攻撃側の判定はプラグインの領域計算で行う
- **ギミック**: 段階変化、地形の使用、条件付きの無敵解除などはすべてプラグイン側のロジックであり、クライアントに依存しない

### 方式A で注意すべき点

| 項目 | 内容 |
|---|---|
| 通信量 | 部位15個 × 更新10Hz × 観戦者20名 = 毎秒3,000件の更新。**変化した部位のみ送る**、更新間隔を10Hzに抑える、遠距離の参加者には間隔を落とす、のいずれかが必要 |
| 補間 | 補間時間を更新間隔と揃えないと、動きが飛ぶか遅れる |
| 死活管理 | 表示エンティティは自然消滅しない。討伐・失敗・サーバー停止のいずれでも確実に除去する処理を持つ |
| リソースパック | 未適用のプレイヤーには何も見えない。**適用を必須とし、未適用者はレイド次元へ入れない** |

### 方式B を採らない理由

クライアントmodを配布すると、レイドは「modを入れた人だけの催し」になる。レイドを個人単位・国籍不問としたのは新規プレイヤーの参加経路を確保するためであり（§12.1）、参加障壁を上げる方式はその目的と衝突する。加えて、Minecraft のバージョン更新ごとにmod側の追随が必要になり、§20.5 の版管理の負担が増える。

### 未確認事項

- **26.2 における表示エンティティとアニメーション関連の変更**。本検証の執筆時点で確認できていない。より直接的なアニメーション手段が追加されていれば、方式A の実装量は下がる
- 部位数と更新頻度の上限は実測で決める。上表の見積りは目安である

### 実装済みの範囲

骨格・モーション・段階・通信量の定義と検証は `core` に実装した（`jp.mcserver.core.raid`）。個体定義を読み込んだ時点で構造の破綻を検出し、必要な更新間隔を算出できる。残るのは表示エンティティの生成と変換の送信であり、これは実機を要する。
