# 実装の実現性検証

> `minecraft_server_spec.md` の各規定について、既存プラグインで足りる範囲と自作が必要な範囲を切り分けた検証。
> 節番号は仕様書のものを指す。

---

## 0. 結論

| 判定 | 意味 |
|---|---|
| **A** | 既存プラグイン、データパック、バニラ機能で足りる |
| **B** | 自作が必要。API上の実現方法が明確で、難度は低い |
| **C** | 自作が必要。挙動そのものを作る必要があり、**先にプロトタイプで確認すべき** |

| 領域 | 判定 | 根拠 |
|---|---|---|
| 次元ワイプ（§1.1） | A | ワールドの unload → リージョンファイル削除 → 再生成。既存のマルチワールド系プラグインの組み合わせで足りる |
| 除外要素（§1.4） | A | レシピ削除、スポーン抑制、ルートテーブル編集 |
| 保護区（§1.2） | A | ワールドガード系の領域保護 |
| 日次exp上限・倍率（§2） | B | exp変動イベントの捕捉と加算の書き換え |
| **有効活動時間（§2.1）** | **B** | pitch変化の検出。下記1を参照 |
| **『消滅の呪い』の全面付与（§3）** | **B** | 付与経路の網羅が本質。下記2を参照 |
| **領土システム（§4）** | **B（最大工数）** | 数式・ランク・チャンク解放・座標告知。既存の土地保護プラグインでは表現できない |
| 役職・権限・貢献度（§6） | B | データ管理のみ |
| 国庫・外交・統一（§7〜8） | B | データ管理と定期処理 |
| 制裁（§10） | B | 定期徴収とDiscord連携 |
| **戦争競技（§11）** | **C** | ボールの物理挙動。下記3を参照 |
| Hub と GUI市場（§12〜13） | B | オーダーブック型の板取引は既存の商店系プラグインでは代替できない |
| シュルカーの国家バインド（§14） | B | アイテムのNBTによる所属記録と拾得制限 |
| Mod検出の統計監査（§18.4） | B | 設置レート・移動整合の集計。完全な検証は不可能という前提（仕様側に明記済み） |

**自作が避けられない中核は、領土システム（§4）とGUI市場（§13）とボール物理（§11）の3点である。** 他はほぼ既存資産の組み合わせで足りる。

---

## 1. 有効活動時間の pitch 判定（§2.1）

### 実装案

仕様は「12分の窓内で pitch の総変化量が 0 なら非計上」と定める。総変化量の**値は使われない**（0か否かのみが判定に使われる）ため、実装は累積ではなく**真偽フラグ**で足りる。

```
窓ごとに boolean moved = false
pitch が変化したら moved = true（以降は何もしない）
12分経過時: moved なら12分を計上。いずれの場合もフラグと窓をリセット
```

これは O(1) であり、プレイヤー数に対して線形にしかならない。

### 検出手段の選択

| 手段 | 長所 | 短所 |
|---|---|---|
| 移動イベントの捕捉 | 取りこぼしがない | 発火頻度が高い。ただし `from.pitch != to.pitch` の即時判定のみなら負荷は無視できる |
| 一定間隔のサンプリング（1秒ごと） | 負荷が最小 | 1秒以内に動かして元の値へ正確に戻す操作を取りこぼす |

**推奨は移動イベント**。サンプリングは、視点を動かして正確に元の角度へ戻すマクロを取りこぼす経路が原理的に残る。

### 最初に確認すべきこと

> **移動イベントが「位置は変わらず視点のみ変化した」場合にも発火するか。**

発火するなら実装は数十行で済む。発火しない実装系であれば、20tick ごとのサンプリングに切り替え、判定間隔を短くして取りこぼしを詰める。**この一点だけは、他の実装に先立って確認する価値がある**（仕様の4本柱のうち1本が乗っているため）。

### 付随して必要なもの

- セッション跨ぎでの状態管理（経過時間は繰り越し、フラグはリセット — §2.1 に規定あり）
- 同一IPの丸め込み。接続元アドレスは取得できるが、CGNAT・IPv6・モバイル回線での誤爆は避けられない。**申請制ホワイトリスト（§2.1）が実装上の必須要件である**

---

## 2. 『消滅の呪い』の全面付与（§3）

付与経路の網羅がすべてである。漏れが1経路でもあれば、そこが蓄積の抜け道になる。**開始延期の基準（§19.7）に挙げてあるのはこの理由による。**

| 経路 | 捕捉点 | 備考 |
|---|---|---|
| エンチャントテーブル | エンチャント確定イベント | 結果に呪いを追加 |
| 金床（本＋道具） | 金床の結果生成イベント | **エンチャントが増えた場合のみ**付与。修理・命名では付与しない（§3） |
| 司書村人の本 | 上記の金床経路で捕捉される | 本そのものへの付与は不要 |
| 村人が売る既エンチャント装備 | 取引結果の取得時 | 武器屋・防具屋の完成品 |
| 構造物チェスト | ルートテーブル生成イベント | データパックでの直接編集でも可 |
| モブドロップ | 同上 | |
| 釣り | 同上 | エンチャント本が出る |
| 交易以外のNPC由来（行商人等） | 取引結果の取得時 | 対象があるか要確認 |

### 有利に働く挙動

**砥石は消滅の呪いを除去できない**（バニラの仕様）。したがって「呪いを外して蓄積する」経路は最初から存在しない。仕様の意図と実装が一致している。

### 検証方法

上記8経路それぞれについて、実際に取得して呪いの有無を確認するチェックリストを作り、D-7 に実行する（§19.2 に作業として記載済み）。

---

## 3. 戦争競技のボール物理（§11）

**唯一、判定 C の項目である。** ここだけは仕様を読んで実装できるとは言えない。

### 名称の問題

仕様に登場する「サルファーキューブ」は、**バニラの Minecraft に存在しない**。仕様書側で「本サーバー独自のカスタムエンティティである」と明記する必要がある（下記5参照）。

### 物理値の出所

§11.5 の球種表が持つ弾性・摩擦の値（0.0〜0.9 / 0.05〜2.0）は、バニラのブロック特性とは対応しない。氷は滑るが、木材・石・キノコブロックの摩擦はバニラでは同一である。したがってこれらは**ラウンドごとにプラグインが設定する独自パラメータ**として実装するほかない。

### 実装案

| 要素 | 手段 |
|---|---|
| 見た目 | ブロック表示エンティティ（クライアント側で補間されるため滑らかに動く） |
| 当たり判定 | 相互作用エンティティを重ねる |
| 移動 | 毎tick、速度を積分。摩擦で減衰、壁・床で反発係数を適用 |
| 打撃 | 攻撃イベントを捕捉し、ダメージ量に比例した速度を加算（§11.5 の注記どおり） |
| 押し出し | 接触による移動はダメージに依存しない（同注記） |

モブ（マグマキューブ等）を使う案は、AI・重力・ノックバック計算がバニラ実装に引きずられるため採らない。表示エンティティ＋自前の積分が制御しやすい。

### 負荷

ボールは1試合に1個であり、20Hz の積分は無視できる。**負荷の懸念はなく、懸念は挙動の調整だけである。**

### 順序

§20「実測後に調整する項目」の5項目のうち3項目（ゴーリーのゾーンボーナス、球種ローテーション、ハニカムラウンド）が、このプロトタイプの完成を前提にしている。**戦争競技は他の実装と独立しているため、領土システムと並行して着手できる。**

---

## 4. 着手順序

| 順 | 対象 | 理由 |
|---|---|---|
| 1 | pitch 判定の発火確認（§2.1） | 数時間で終わり、結果によって実装方式が変わる |
| 2 | 領土システム（§4） | 最大工数。他の多くが依存する |
| 3 | 『消滅の呪い』の経路網羅（§3） | 開始延期の基準に直結 |
| 4 | 次元ワイプと Hub（§1.1・§12） | 既存資産の組み合わせ |
| 5 | GUI市場（§13） | Hub の完成後 |
| 6 | ボール物理のプロトタイプ（§11） | 独立。並行可能 |
| 7 | 外交・統一・制裁（§8・§10） | rank5 以降にしか使われないため、開始後の実装でも間に合う |

**§8 と §10 は開始時点で未実装でも運営が成立する。** 同盟の解禁は双方 rank5、制裁は共和制（rank15）が前提であり、最短でも開始から数日〜2週間の猶予がある（§19.5 の到達日程）。

---

## 5. 仕様書側に反映すべき事項

| 事項 | 内容 |
|---|---|
| サルファーキューブの定義（§11.2） | バニラに存在しない名称であるため、独自エンティティであることを明記する |
| pitch 判定の実装注記（§2.1） | 総変化量の値は使われず「変化したか否か」のみが判定に用いられる旨を注記する |
| 同一IP判定の限界（§2.1） | CGNAT・モバイル回線での誤爆が原理的に避けられないため、ホワイトリスト申請が必須の運用要件であることを明記する |
