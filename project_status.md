# 現況まとめ

決まったこと・決まっていないことを一望するための紙である。**詳細は各仕様書にあり、ここは索引と状態の一覧に徹する。**

最終更新の基準: レイド個体「騎士型」の外見・モーション・判定が実機で通り、塗り絵も入った時点。**騎士型は一巡した。**

---

## 1. 成果物の地図

| 場所 | 役割 | 状態 |
|---|---|---|
| `minecraft_server_spec.md` | サーバー全体の設計仕様。§1〜23 と付録 | 設計は確定。実装は未着手の領域が多い |
| `raid_species.md` | レイド個体の設計。騎士型（第一・第二形態）の骨格・モーション・数値 | 騎士型は確定。**虚刃の衛士（2種目）は実体3モーションを実装済み**（尺・骨格は決定、特殊4モーションと段階移行は未着手）。他3種は未着手 |
| `raid_model_spec.md` | モデルを描く側の規定。座標系・原点・寸法表・生成手順 | 較正・向きのずれの解決まで完了。**騎士型・虚刃の衛士の2種目ぶんの雛形を生成**。騎士型は絵も描き込み済み、虚刃の衛士は雛形のみ（塗り絵は未着手） |
| `implementation_feasibility.md` | 仕様のうち実装で詰まる箇所の切り分け | 主要な論点は整理済み |
| `local_test_setup.md` | Windows での検証環境の作り方と確認項目 | 手順は通っている |
| `capacity_plan.md` / `mod_rulings.md` | 収容計画 / Mod の判例集 | 初版のまま |
| `rail_infra_spec.md` | 地下鉄インフラ管理・経済連携プラグインの要件定義（Vault・Towny/Lands 連携） | **要件定義のみ。実装は未着手**（`core`/`plugin` ともコード無し） |
| `core/` | サーバー非依存のロジック（Java、外部依存なし） | 検証 **757 件** 成功 |
| `plugin/` | Paper 1.21.4 の検証用プラグイン | ビルド・読み込みは通る。中身は騎士型と村人の取引のみ |
| `resourcepack/` | リソースパック。較正用の立方体、生成した雛形、塗り絵 | 雛形 **騎士型29件 + 虚刃の衛士7件**。**塗り絵は騎士型11枚が描き込み済み、虚刃の衛士5枚は下地のみ**（手順は `PAINTING.md`） |

---

## 2. 決まったこと

### 2.1 村人の取引（§3.2）

> **村人および行商人は、エンチャント本およびエンチャント済みの品を提供しない。**

削除した枠は、次の代替表に定めたものを除き埋め合わせを置かない。**割り当ては固定**であり、職業の付け替えによる引き直しに意味がない。

| 職業 | 村人レベル | 代替 |
|---|---|---|
| 防具鍛冶 | 4 熟練 | 耐久力I の本 |
| 防具鍛冶 | 5 達人 | ダメージ軽減I の本 |
| 道具鍛冶 | 3 一人前 | 耐久力I の本 |
| 道具鍛冶 | 4 熟練 | 効率強化I の本 |
| 道具鍛冶 | 5 達人 | 幸運I の本 |
| 武器鍛冶 | 5 達人 | ダメージ増加I の本 |
| 矢師 | 4 熟練 | 効能付きの矢×5 |

- 値段は司書のバニラ式（レベルI なので **5〜19 エメラルド＋本1冊**）。個体ごとに固定
- 1レベルにつき代替は1枠まで。司書には代替を置かない
- 実装済み・実機で確認済み

### 2.2 レイド個体「騎士型」— 戦場と判定

| 項目 | 値 |
|---|---|
| 戦場 | 召喚位置を中心とした**半径30ブロックの円筒** |
| 攻撃を受け付ける距離 | **個体から10ブロック以内で放たれたもの**のみ。矢は発射地点で見る |
| 受け付けない武器 | **トライデント・メイス** |
| 接近を止める距離 | **3.5ブロック**手前で止まる。密着まで踏み込まない |
| 当たり判定の測り方 | プレイヤーを**足元〜背丈1.8の縦の線分**として、武器の点から **2.2ブロック**以内 |
| 全攻撃の押し出し | **0.1**（技ごとの押し出しがあればその上に足す） |
| 向きの追従 | 1回の姿勢更新で **15度**まで |
| 攻撃後 | **相手を向いてから**待機を数えはじめる（上限40tick） |
| 待機 | 標準 **80 tick**。中断による隙は 3段突き100 / 踏みつけ80 |

### 2.3 騎士型 — モーション

| 技 | 形態 | 長さ | 要点 |
|---|---|---|---|
| 突進切り上げ | 両方 | 52 tick | 0.5ブロック後ずさり → 30tickで毎秒18ブロックまで加速 → 20ブロック走る。**パリイでしか止まらない。追尾しない** |
| なぎ払い | 両方 | 18 tick | ヨー回転で薙ぐ |
| 3段突き | 両方 | 70 tick | 3回突く。65tickまでに槍を叩けば中断 |
| 追従4連切り | 両方 | 65 tick | 突き→振り下ろし→突き→振り下ろし |
| 大ジャンプ衝撃波 | 両方 | 95 tick | 20ブロック跳び中心へ着地。半径10の波が毎秒10ブロックで広がる。**戦場の外にいるときだけ** |
| 回旋突進 | 第二 | 334 tick | 下記 |
| 踏みつけ | 第二 | 20 tick | 衝撃波。2人以上のとき |

**回旋突進**（第二形態の大技）

| 段 | tick | 内容 |
|---|---|---|
| 回旋 | 0〜298 | **戦場の外周**（半径30）を1.5周。毎秒15で走り出し、**最初の半周で毎秒20**へ。約15秒 |
| 突進 | 298〜334 | 毎秒20で走り出し20tickで毎秒24。走破 **41ブロック** |
| 追尾 | 毎tick | 進行方向の**左右8度以内**で最も手前の相手へ寄せる |
| 中心の通過 | 毎tick | **中心から20ブロック以内（直径40の回廊）を必ず通る**。回廊は追尾より優先 |

### 2.4 騎士型 — パリイ・弱点・激昂

- **パリイは盾では成立しない。** 区間内の累積ダメージで判定し、1回目 **10**、以降 **+10**
- 弱点は**頭のみ**、倍率 **×2.5**。パリイ・妨害・空振りの直後 60 tick（空振りは40）だけ開く
- **激昂**は個体の攻撃が 400 tick 命中しないと発動。待機15tick・ダメージ1.2倍・弱点が閉じる

### 2.4b 騎士型 — 盾のガードと待機中の追従

| 項目 | 値 |
|---|---|
| ガードの軽減量 | **全ブロック**（バニラ準拠） |
| ガードが成立する向き | 視線から左右 **90度** 以内 |
| ガードを貫通する技 | **衝撃波だけ**（踏みつけ・大ジャンプ衝撃波）。試験がこの対応を骨格データに固定している |
| ガード中のノックバック | **残る** |
| 盾の耐久 | 受けたダメージぶん減る（3未満では減らず、それ以上は 1+整数部）。強度は1点ずつ間引く |
| ガードの空振り扱い | しない（「当てた」に数える。盾を構えるだけで弱点が開かないようにするため） |
| パリイ | 盾では成立しない（従来どおり） |
| 待機中の追従の遅れ | **10 tick**。接近中と技の最中は遅らせない |

**当たり判定の太さは、見た目より太いままで通す（決定）。** 判定は分割後の長辺を一辺とする立方体なので、腕（太さ 0.40・長さ 1.10）の判定は 1.10 角になる。**直さない。**

| 理由 | 内容 |
|---|---|
| 損をするのは個体の側だけ | 部位の判定は**プレイヤーが殴る先**である。個体の攻撃が届く範囲には効かない（判定区間とリーチ 2.2 で別に測っている） |
| 狙って当てられることを優先 | 寸法どおりにすると「見えているのに当たらない」が起きる |
| 直すと分割が増える | 判定は毎 tick 追従するため、分割数がそのまま毎秒の位置更新に効く |

---

### 2.5 開催と参加（§12.1 / §12.3）

| 項目 | 値 |
|---|---|
| 開催間隔 | 2週間（隔週） |
| 同日の開催枠 | **06:00 から3時間ごとに6枠** |
| 参加 | **同じ開催日では1回のみ**（成否を問わない）。延べ72名 |
| 最大参加人数 | **12名**（描画負荷から引いた線） |
| 基準体力 | 参加1名あたり 600 |
| 制限時間 | 40分 |

### 2.6 モデルと描画（`raid_model_spec.md`）

**較正は完了している。**

| 項目 | 結果 |
|---|---|
| 原点 | **モデル座標 (8,8,8)**。想定どおり |
| 向き | **ItemDisplay はモデルを Y軸まわりに180度回して描く**。描画側で戻している |
| 縮尺 | 1ブロック=16単位。座標は −16〜32。**槍・虚刃の衛士の剣は 1/4 で描き、描画側が4倍に戻す**（座標に収まらない長い部位はこの考え方で種目を問わず対応する） |
| 振り分け | 1.21.4 の**アイテム定義**（`assets/minecraft/items/paper.json` の `range_dispatch`）。`overrides` は削除されている |

雛形は**骨格データから生成する**（`./core/generate-pack.sh`）。寸法表を手で写さないため、当たり判定と見た目の寸法が食い違わない。生成した JSON は Blockbench でそのまま開ける。

**見た目づくりは「PNG を塗るだけ」に落とし込んである。**箱の6面を1枚の画像に展開した割り付けを生成側が決め、その枠を UV としてモデルへ書き込むため、立体を扱う道具は要らない。

| 決め事 | 値 |
|---|---|
| 塗る場所 | `assets/minecraft/textures/item/knight/*.png`（**11枚**）、`assets/minecraft/textures/item/hollow_guard/*.png`（**5枚**、`guard_` 始まり） |
| 1枚の大きさ | 32×32 |
| 共有 | 左右の腕・角・肩、両形態の同じ部位は1枚。寸法差は UV の伸びに吸収させる |
| 枠の目印 | `F` 前 / `B` 後 / `R` 右 / `L` 左 / `U` 上 / `D` 下。塗り替えれば消える。狭くて入らない枠は `templates/guide/` の4倍図で読む |
| 細かさ | 1ブロックあたり6〜39画素（バニラは16画素）。胴 11・槍 8・馬胴 6 は**バニラより粗い** |
| 生成し直したとき | モデルと原本（`templates/`）は上書き、**塗った PNG は触らない**（大きさが画布と違えば警告） |

方式の切り替えは**実行中にできる**（`/raid model authored` / `/raid model vanilla`）。骨格は個体を作るときに組むため、コマンドは出ている個体をその場で作り直す。既定はバニラの素材で、サーバーを起動し直すとそこへ戻る。**騎士型・虚刃の衛士は1つのスイッチで揃って切り替わる**（`KnightDefinition`・`HollowGuardDefinition` を同時に切り替える）。

---

## 3. 実装の状態

| 層 | 実装済み | 未実装 |
|---|---|---|
| `core` | 領土・経済・外交・議会・市場・レイドの開催と選択・骨格と変換・モーション・当たり判定の幾何 | 永続化、GUI の生成、スケジューラ |
| `plugin` | 騎士型の生成・モーション・判定・パリイ・弱点・激昂・段階移行・盾のガード・ドロップ品、村人の取引、**『消滅の呪い』の全面付与**、**開催の一巡（登録・告知・開始・制限時間）**、**レイド専用次元**、**虚刃の衛士（実体3種・特殊4種・第三形態の金のオノまで実装、実機で複数回検証・修正中）**、検証用コマンド | それ以外のすべて（領土・経済・外交・市場・Hub） |
| リソースパック | 較正用の立方体、部位ごとの箱モデル **騎士型29件 + 虚刃の衛士7件**、振り分け（両種目まとめて1つ）、塗り絵**騎士型11枚（描き込み済み）+ 虚刃の衛士5枚（下地のみ）** | 箱より複雑な形（角錐・曲面の作り込み）、虚刃の衛士の塗り絵 |

**検証用コマンド**: `/raid spawn` `/raid despawn` `/raid info` `/raid god`（体力を減らさない） `/raid calibrate`（原点の較正） `/raid dump`（送っている変換を数値で出す） `/raid model authored|vanilla`（見た目の方式を切り替えて出し直す） `/raid axes`（向きの分かった十字で描画のずれを切り分ける）

---

## 4. 未確定・未解決

### 4.1 解決した不具合

**描画が当たり判定と 90 度／180 度ずれ、下を向いて見えた問題は解決した。**

原因は**原点の `Location` に召喚時のプレイヤーの向きが残っていた**ことである。表示エンティティは
自身のエンティティ向き（yaw / pitch）でも回転するのに対し、当たり判定は `Interaction` の
軸に沿った箱で向きの影響を受けない。そのため**表示だけが召喚時の向きぶん余計に回っていた**。

| 見えていた症状 | 実際 |
|---|---|
| 90 度／180 度のずれ | ずれ量は召喚時にプレイヤーが向いていた方角そのもの |
| 下を向いている | 召喚時のプレイヤーの pitch |
| `/raid dump` は判定と一致する | dump は送った変換を出す。エンティティ向きの回転は現れない |
| 再現しないことがある | 向き0で召喚したときだけ一致する |
| −45 度の傾きで多少ましになった | 45 度あたりを向いて召喚していたときの対症 |

- 直し方: `BossRig.upright` が原点から向きを落とす。較正の道具（`Calibration` / `Axes`）も同じ関数を通す
- `MODEL_TILT_DEGREES` は **0 に戻した**。合成の計算からも余分だと確認済み
- 再発の見張り: `/raid dump` が**表示エンティティ自身の向きの最大値**を出す。0 以外なら再発
- 切り分けに使った道具: `/raid axes`（向きの分かった十字を部位と同じ手順で出す）、`/raid calibrate`（補正の内訳を4通り並べる）

### 4.2 仕様は決まっているが未実装

| 項目 | 状態 |
|---|---|
| **騎士型のドロップ品** | 下記 4.5 に全文。実装は外見とモーションの完結後 |
| ~~『消滅の呪い』の全面付与（§3.1）~~ | **実装済み。** 6経路（テーブル・金床・チェスト・モブ・釣り・拾得）。検証表は `local_test_setup.md` |
| ~~参加登録~~ | **実装済み。** 登録・告知・締切・開始・制限時間・終了の一巡。単身討伐許可証も使える |
| ~~レイド専用次元~~ | **実装済み。** 配布ワールド `raidboss` を会場にし、設置・破壊・PvP を禁止。参加者は半径13以内へ、個体は高さ3から落下して北向きに着地（20秒後） |
| ~~レイドの取り巻き~~ | **全種で不採用と決定**（§12.3）。スケールするのは体力だけになった。人数が増えるほど楽になる一方なので、体力倍率は §22 の調整対象 |
| 有効活動時間の pitch 判定（§2.1） | 実装案は整理済み。未着手 |
| 領土・経済・外交・市場・Hub | `core` にロジックはある。プラグイン層が未着手 |

### 4.3 仕様が未定

| 項目 | 内容 |
|---|---|
| レイド個体 2〜5種 | 初回リリースは5種。**騎士型のみ確定**、残り4種は未着手 |
| Hub の建築 | §13 は寸法・配置・上限のみ。実際の建築とテクスチャ選定 |
| 判例集の確認 | `mod_rulings.md` は執筆時点の把握に基づく。公表前に各Modの現行版を確認 |
| Folia 移行 | スケジューラとスレッド安全性。未着手 |

### 4.4 実測後に決める（レイド関連の抜粋）

§22 に全項目がある。特に判断待ちのもの:

| 項目 | 現状と論点 |
|---|---|
| **回旋の速さ** | 実機で「**1.5倍でも大丈夫**」との体感を得ている。上げると突進の初速との連続性が崩れる |
| **激昂までの停滞** | 待機を80tickに延ばしたぶん激昂が早まり、12名の再現では**開始20秒で発動**した。閾値 400→480 か、待機中は数えないか |
| 大ジャンプの衝撃波のダメージ | **35 は仮置き** |
| 効能付きの矢の効能 | 6種は**仮置き** |
| 攻撃を受け付ける距離 | 10ブロックで弓が実質使えなくなっていないか |
| 突進の追尾角 | 左右8度で避けられるか |
| 中心の回廊 | 直径40。広げると走破距離41との兼ね合いで戦場の外へ出はじめる（境界は半径 約22.4） |
| 武器の判定の余裕 | 2.2ブロックが広すぎないか |

### 4.5 ドロップ品（**実装済み**）

仕様と実装は `raid_species.md` の「ドロップ品」に移した。ここには残りだけ書く。

| 残り | 内容 |
|---|---|
| 本物の旗模様・鍛治型 | データパックで `banner_pattern` / `trim_pattern` を登録する必要がある。いまはバニラの品を借りて名前と付け札を載せている。付け札で正体を持たせてあるので、差し替えは局所で済む |
| 単身討伐許可証の**使い道** | 品は配れるが、「開催期間の外でソロで挑む」経路そのものは参加登録（§12.1）の実装待ち |
| 受け取り手が不在のときの取り置き | いまは記録だけ。永続化の話になる |
| 討伐 exp・称号・国家バフ（§12.4） | ドロップ表とは別枠。開催の形（§12.1）と一緒に作る |

---

## 5. 検証の状況

| 手段 | 状態 |
|---|---|
| `./core/run-tests.sh` | **829 件成功**。Bukkit に依存しないロジックを網羅 |
| `./core/simulate-knight.sh` | 戦闘をオフラインで再現。12名・8DPS で討伐 約1分 |
| `./core/dump-rig.sh` | 表示へ送るべき配置を数値で出す |
| `./core/sketch-knight.sh` | 骨格の略図（SVG） |
| 実機（Paper 1.21.4） | 村人の取引・移動・接地・パリイ・弱点・激昂・突進・外見・盾のガード・待機中の追従・**『消滅の呪い』の全経路**まで確認済み。**ドロップ品・開催の一巡・レイド専用次元・虚刃の衛士は未確認**（`local_test_setup.md` に確認表あり） |

**私の環境では Paper API を取得できない**（プロキシが `repo.papermc.io` を弾く）ため、プラグインのコンパイルはスタブでしか確認できない。実機のビルドが唯一の検証である。有効化ログに jar の日時を出すようにしてあるので、**症状を報告するときはその日時も添えると取り違えが防げる**。

---

## 6. 次の一手の候補

**騎士型は一巡した。** 外見・モーション・判定・盾・段階移行が実機で通っている。次はレイドを
「遊べる一式」にする側の作業になる。

1. **虚刃の衛士を実機で検証する** — 剣の免疫・Y座標・軌道の当たらなさ・盾で防いでも消えない・
   地形をすり抜ける、と複数回の実機検証で見つけて直してきた。第三形態（金のオノ）も実装済み
   だが実機未確認。残るのは、ここまでの修正が実際に効いているかの確認と、浮遊剣・オノの
   見た目（自転でごまかしている向き）・戦場との整合をレイド専用次元で確かめること
   （`local_test_setup.md` に確認事項を追記済み）
2. **レイド種3〜5の設計**（`raid_species.md`）— 騎士型・虚刃の衛士で型が2つ揃ったので、同じ骨組みで増やせる
3. **§22 の調整候補**（旋回速度 ×1.5、激昂の膠着 400→480、跳躍波 35、矢の効果一覧）— 実機の体感が溜まってから

**`KnightBoss` と `HollowGuardBoss` の重複は解消済み。** `RaidBossBase` へ括り出し、種ごとの
違いを骨格（`RaidSpecies`）・定数（`Tuning`）・効果音の3つに絞った（§2「実装の状態」）。
括り出す過程で、虚刃の衛士に欠けていた共通の規約2点（中心経由・待機中の追従の遅れ）も揃った。

**虚刃の衛士の特殊4モーション（浮遊する剣）も実装済み。** 実体系統とは独立した小さな状態機械
（`SpecialTrack`）を、`RaidBossBase` に足した空の `tickSpecial()` フック1つで並行させた
（「2系統並行の状態機械」という、当時唯一残っていた構造上の課題への答え）。
浮遊剣は `BossRig` の部位ツリーに属さない独立した表示エンティティとして持ち、当たり判定も
サーバー側の距離判定（衝撃波と同じやり方）で取っている。

**実機での初回検証で2件の不具合が見つかり、修正済み。**

- 剣の当たり判定が体に比べて大きく、免疫（`immune()`）のままだとほとんどの攻撃が剣に吸われて
  実質ダメージが通らなかった。免疫ではなく20%軽減（`Rig.Part#reducedDamage`）に直した
- 特殊4種のY座標が絶対座標のままで、レイド専用次元（会場の足元Y=1）以外へ召喚すると高度が
  合わなかった。個体の召喚位置の足元Yを基準にした相対値に直した（`SwordRain` /
  `SpatialSlash` / `GrandWhirl` の各オフセット定数）

**2回目の実機検証では、特殊4種が「まったく命中しない」ことが分かった。** 空間斬撃・全域大旋回は
戦場の中心を軸とした円弧で、旋回を始めた時点で軌道が固定されるため動く相手にほぼ当たらない
設計だった。狙った相手へ向けて直進し、移動開始から80tickのあいだ10tickごとに狙いを更新し
直す方式（`HomingBlade`、`core` の `HomingDart`）に作り直した。速度25→30m/s、本数・発生間隔も
参加人数×3・5tickごとに5本へ揃え、円弧専用だった `ArcSweep` は削除した。降り注ぐ刃も、落下中に
水平位置を相手の現在位置へ寄せ続けるよう追尾を加えた。あわせて、色付きブロックでは「剣と
分からない」との指摘を受け、浮遊剣の見た目を実際の剣アイテム（銅だけ木の剣で代用）へ
差し替えた——バニラの剣アイテムの姿勢は実機較正していないため、飛んでいる剣は自転させて
向きの不確かさを見せないようにしている。

**3回目の指摘では、盾で防いでも浮遊剣が消えない・軌道を直した全域大旋回をさらに強くしたい・
第三形態（金のオノ）が欲しい、の3点が挙がった。** `independentHit` が「盾に防がれたか」を
返すようにし、防がれた浮遊剣3種（`RainBlade`・`HomingBlade`・`SpikeBlade`）はその場で消える
ようにした。あわせて `HomingBlade` に地形との当たり判定（`solidAt()`）を新設した——単純に
既存の `groundY()` を流用すると、地面が近くに無い場所では「渡した位置をそのまま返す」ため、
上空を飛んでいる最中につねに「もう地面に着いた」と誤判定して移動開始と同時に消えてしまう
罠があり、避けた。全域大旋回は本数を参加人数×5、1回の発生を10本、総移動距離を130ブロックへ
（空間斬撃とは別の値に）強化した。体力33%で入る第三形態を新設し、金のオノ（`GoldenAxe`、
ダメージ10・速度25m/s、命中すると通常のオノと同じく盾を一時的に使えなくする）を特殊4種
すべてに追加した——降り注ぐ刃・空間斬撃・串刺しは既存クラスを素材・ダメージだけ変えて
流用し、全域大旋回だけは素材の並びにオノを織り込んで周期を5→6にする形にした。

**4回目の実機検証では、「空間斬撃・全域大旋回が対応できない」「降り注ぐ刃が一度も召喚
されなかった」の2点が挙がった。** 空間斬撃・全域大旋回は、狙った相手の位置に召喚する方式を
やめ、**虚刃の衛士自身の頭上**（地表面+4）という固定点に直した——相手の足場によって出現
位置が安定しなかったため。待機も延ばし（空間斬撃30tick・全域大旋回40tick）、**命中した瞬間
（防がれたかに関わらず）その場で消える**単発の一撃に直した。降り注ぐ刃は、3回目で
`HomingBlade` に見つけたのと同じ「`groundY()` を着地判定にそのまま使うと出現直後に誤判定
して消える」罠が `RainBlade` にも実際に入り込んでいたと判明し、一度も目に見える形で
落下せずに消えていた。`solidAt()` を使う形に直した。

**5回目の指摘は「悪くない」という前置きのうえで、待機のランダム化とターゲットの均等分散の
2点だった。** 生成してから移動を始めるまでの待機（空間斬撃・全域大旋回・串刺しは技ごとに
固定値、降り注ぐ刃はそもそも無かった）を、共通の `SpecialWaveTiming`
（`MIN_WAIT_TICKS=30`〜`MAX_WAIT_TICKS=80`）を使ったランダムへ統一した——「すべての
特殊技」という指示を4種すべてに及ぶものと読み、それまで待機フェーズを持たなかった
降り注ぐ刃にも新しく待機フェーズを加えた。**金のオノだけは `AXE_WAIT_TICKS=90` の固定値**
のまま残す（指定どおり）。狙う相手も、生成のたびの乱数選択（`SpecialTrack`）から、戦場に
いる全員へ1バーストごとに0から順番に割り振る `nextTarget()` 方式へ直した——1人テストの
環境では「集中しない」効果自体を実機で確認できないため、複数人参加時の分布は未確認のまま。

引き続き実機での確認が必要。
