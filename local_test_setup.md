# ローカル検証環境の構築手順

レイド個体（§12）とプラグインを、手元のPCだけで確認するための手順。**インターネット公開は不要**で、同一PC内の `localhost` 接続で完結する。

---

## 0. 前提

> **シングルプレイでは検証できない。** プラグインが動くのは Paper サーバーのみである。

手元のPCに Paper サーバーを立て、同じPCの Minecraft クライアントから `localhost` に接続する。これが「オフラインでの実機検証」の実体になる。

| 必要なもの | 備考 |
|---|---|
| Java 21 以上 | Temurin など。`java -version` で確認 |
| Paper の jar | **サーバーで動かす対象バージョンに対応したビルド**を選ぶ |
| 空きメモリ 4GB | 検証用途では十分 |
| Minecraft クライアント | サーバーと**同じバージョン**であること |

---

## 1. サーバーの配置

```
raid-test/
├── paper.jar
├── start.sh        （Windows なら start.bat）
└── plugins/
    └── raid-plugin.jar
```

**start.sh**

```sh
#!/bin/sh
java -Xms2G -Xmx4G -XX:+UseG1GC -jar paper.jar nogui
```

**start.bat**

```bat
@echo off
java -Xms2G -Xmx4G -XX:+UseG1GC -jar paper.jar nogui
pause
```

---

## 2. 初回起動と設定

1. `start.sh` を実行する。`eula.txt` が生成されて停止する
2. `eula.txt` を開き `eula=false` を **`eula=true`** に書き換える
3. もう一度起動する。ワールドが生成される
4. 一度停止し、`server.properties` を検証向けに調整する

| 項目 | 値 | 理由 |
|---|---|---|
| `online-mode` | `false` | 認証を経由せず接続できる。**ローカル検証専用**。公開サーバーでは必ず `true` |
| `difficulty` | `hard` | ダメージ量を本番と揃える |
| `spawn-protection` | `0` | 検証時にブロックを置けるようにする |
| `view-distance` | `10` | 表示エンティティの追従距離の確認に十分 |
| `max-players` | `20` | レイドの上限（§12.1）に合わせる |
| `gamemode` | `creative` | 検証者はクリエイティブで観察する |

---

## 3. 接続

クライアントの「マルチプレイ」→「サーバーを追加」→ アドレスに **`localhost`** を入力する。同一PCなので待ち時間はほぼない。

---

## 4. プラグインの入れ替え

1. サーバーを**停止**する
2. `plugins/` の jar を差し替える
3. 起動する

> `/reload` は使わない。表示エンティティやスケジューラを持つプラグインでは、古い実体が残って原因の切り分けができなくなる。

---

## 5. リソースパックの適用

レイド個体のモデルはリソースパックに入る（§12.6 方式A）。ローカル検証では2通りある。

| 方法 | 手順 | 備考 |
|---|---|---|
| **手動適用**（推奨） | パックを `.minecraft/resourcepacks/` に置き、クライアントの設定で有効化する | サーバー設定の変更が不要。検証中の差し替えが速い |
| サーバー配布 | `server.properties` の `resource-pack` に **HTTP(S) のURL** と `resource-pack-sha1` を設定する | ローカルファイルのパスは指定できない。本番と同じ経路を確認したい場合のみ |

> **モデルが無くても検証は進む。** 表示エンティティは既定の見た目で出るため、**モーション・当たり判定・段階移行・ノックバックはリソースパック無しで確認できる**。見た目の確認だけを後回しにできる。

---

## 6. 検証のチェックリスト

騎士型（`raid_species.md`）の確認項目。**上から順に、前の項目が通ってから次へ進む**。

### 6.1 表示

- [ ] 部位ぶんの表示エンティティが生成される（第一形態7、第二形態10）
- [ ] 親子関係どおりに追従する（槍が右腕に、四足が馬胴に）
- [ ] 補間が滑らかである（カクつく場合は更新間隔と補間時間が揃っていない）
- [ ] 討伐・失敗・サーバー停止のいずれでも**表示エンティティが残らない**

### 6.2 モーション

- [ ] 突進切り上げ: 構え10tick → 突進20tick、毎秒10ブロック
- [ ] なぎ払い: 210度 → −30度（振り幅240度）を8tickで振る
- [ ] 3段突き: 判定3回、3回目に10tickのディレイ
- [ ] 追従4連切り: 判定4回、槍の先端が対象を追う
- [ ] 回旋突進: 直径30を1.5周（毎秒約28ブロック）→ 突進（毎秒14ブロック）

### 6.3 戦闘

- [ ] ダメージ量が定義どおり（突進25〜30、3段突き20×3 ほか）
- [ ] ノックバックが上3・後5（第二形態の回旋突進は後7）
- [ ] 槍にはダメージが通らない
- [ ] **妨害**: 突進中に槍を攻撃 → 中断して待機80tick
- [ ] **妨害**: 踏みつけ前に頭を攻撃 → 中断して待機40tick
- [ ] **パリイ**: 突進切り上げ・踏みつけで成立する
- [ ] 踏みつけの範囲が半径10・高さ0.3、衝撃波28

### 6.4 進行

- [ ] 行動サイクルが 待機40 → 移動20 → 攻撃 になっている
- [ ] 移動速度が第一形態6.0・第二形態7.0ブロック/20tick
- [ ] 体力50%で第二形態へ移行し、骨格が入れ替わる
- [ ] 参加人数で体力がスケールする（1名600、2名1,140、20名10,860）

### 6.5 負荷

- [ ] `/tps` が 20 を保つ
- [ ] 更新件数が上限（毎秒4,000件）を超えない

> **人数の再現には限界がある。** 手元で20名を集めることは難しい。部位数と更新間隔から見積もった値（§12.6）と、1名時の実測を突き合わせて外挿する。

---

## 7. よくある詰まり

| 症状 | 原因と対処 |
|---|---|
| 起動直後に停止する | `eula.txt` が `false` のまま |
| クライアントが接続できない | サーバーとクライアントの**バージョン不一致**。ポート25565の競合も確認 |
| プラグインが読み込まれない | Java のバージョン不足、または対象バージョン向けにビルドされていない。起動ログの警告を読む |
| 表示エンティティが見えない | 表示エンティティに対応していないクライアントバージョン。または生成位置が視界外 |
| 動きがカクつく | 更新間隔と補間時間が揃っていない。まず更新間隔2tick・補間2tickで揃える |
| モデルが白い箱のまま | リソースパックが未適用。**動作検証はこのままでも進められる** |

---

## 8. 実機を待たずにできること

ロジックだけの検証は Minecraft を起動せずに実行できる。

```sh
cd core
./run-tests.sh                 # 定義と数式の検証
./simulate-knight.sh 20 8 80   # 参加20名・DPS8・軽減80%での戦闘再現
```

段階移行、被弾、討伐時間、モーションのサンプリング、通信量の見積りがその場で確認できる。**実機で確認すべきは「見た目・当たり判定・体感」であり、数値の整合はここで潰しておく。**
