# レイド個体のモデル寸法規定

リソースパックで部位のモデルを描くための規定。数値は `core` の骨格定義（`KnightDefinition`）から生成しており、コードを変えれば本書も更新する。

> **本書は「描く側」の規定である。** 配置・回転・当たり判定はプラグインが持つ。描く側が守るのは**原点と寸法**の2つだけである。

---

## 1. 座標系

| 軸 | 向き |
|---|---|
| +Z | 個体の**正面** |
| +Y | 上 |
| +X | 個体から見て**左** |

**「右」の部位は x が負である。** Minecraft では正面が +Z のとき、その実体から見た右は −X になる。骨格の x 座標もこれに合わせてある（`右腕` は x = −0.52）。

> モーションの角度規定（§12.6 の「正面から見て真右を0度」）は**見る側から**の表記であり、こちらとは別の話である。混同しないこと。

---

## 2. 原点（ピボット）

部位には2種類の原点がある。**モデルはこの点を中心に回る。**

| 種別 | 原点の位置 | 該当する部位 |
|---|---|---|
| **中心** | 直方体の中心 | 胴・人胴・頭・肩・馬胴・頭飾り・穂先 |
| **付け根** | 上端の面の中心。ここから**下へ**伸びる | 腕・足・角・槍 |

付け根合わせは、肩から腕が垂れる・股から足が伸びる、という回り方を作るためである。腕を振れば付け根を軸に回る。

---

## 3. 縮尺

**1ブロック = 16単位。** モデルの `elements` はこの単位で書く。

**モデルの座標は −16〜32 の範囲しか取れない。** 48単位＝3ブロックである。これを超える部位は縮めて描き、プラグイン側で等倍に戻す。

| 部位 | 描く縮尺 | 理由 |
|---|---|---|
| 槍以外のすべて | **1:1** | 最長辺が3ブロックに収まる |
| **槍** | **1/4** | 3.40〜3.80ブロックあり範囲を超える。プラグインが4倍に戻す |

- 戻すのは**等倍**である。テクスチャは歪まない
- 槍を1/4で描くと、第一形態で 0.065 × 0.85 × 0.065 ブロック ＝ **1.0 × 13.6 × 1.0 単位**になる

---

## 4. 寸法表

`ID` はモデル識別子（`custom_model_data`）。`判定` は当たり判定の立方体の一辺と個数で、**描く側は意識しなくてよい**（参考値）。

### 第一形態（全長 3.5 / 幅 1.6）

| 部位 | 親 | ID | 原点 | 寸法（ブロック） | 寸法（単位） | 判定 |
|---|---|---|---|---|---|---|
| 胴 | — | 2001 | 中心 | 0.95 × 1.20 × 0.62 | 15.2 × 19.2 × 9.9 | 立方体 1.20 × 1 |
| 頭 | 胴 | 2002 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | 立方体 0.68 × 1 |
| 右角 | 頭 | 2003 | 付け根 | 0.11 × 0.72 × 0.11 | 1.8 × 11.5 × 1.8 | なし |
| 左角 | 頭 | 2004 | 付け根 | 0.11 × 0.72 × 0.11 | 1.8 × 11.5 × 1.8 | なし |
| 頭飾り | 頭 | 2005 | 中心 | 0.60 × 0.60 × 0.60 | 9.6 × 9.6 × 9.6 | なし |
| 右肩 | 胴 | 2006 | 中心 | 0.55 × 0.42 × 0.60 | 8.8 × 6.7 × 9.6 | 立方体 0.60 × 1 |
| 左肩 | 胴 | 2007 | 中心 | 0.55 × 0.42 × 0.60 | 8.8 × 6.7 × 9.6 | 立方体 0.60 × 1 |
| 右腕 | 胴 | 2008 | 付け根 | 0.40 × 1.10 × 0.40 | 6.4 × 17.6 × 6.4 | 立方体 1.10 × 1 |
| 左腕 | 胴 | 2009 | 付け根 | 0.40 × 1.10 × 0.40 | 6.4 × 17.6 × 6.4 | 立方体 1.10 × 1 |
| 右足 | 胴 | 2010 | 付け根 | 0.44 × 1.20 × 0.44 | 7.0 × 19.2 × 7.0 | 立方体 1.20 × 1 |
| 左足 | 胴 | 2011 | 付け根 | 0.44 × 1.20 × 0.44 | 7.0 × 19.2 × 7.0 | 立方体 1.20 × 1 |
| **槍** | 右腕 | 2012 | 付け根 | 0.26 × 3.40 × 0.26 | **1/4で描く**（1.0 × 13.6 × 1.0） | 立方体 0.68 × 5 |
| 穂先 | 槍 | 2013 | 中心 | 0.70 × 0.70 × 0.70 | 11.2 × 11.2 × 11.2 | なし |

### 第二形態（全長 4.6 / 幅 2.0）

| 部位 | 親 | ID | 原点 | 寸法（ブロック） | 寸法（単位） | 判定 |
|---|---|---|---|---|---|---|
| 人胴 | — | 2101 | 中心 | 1.00 × 1.15 × 0.62 | 16.0 × 18.4 × 9.9 | 立方体 1.15 × 1 |
| 頭 | 人胴 | 2102 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | 立方体 0.68 × 1 |
| 右角 | 頭 | 2103 | 付け根 | 0.12 × 0.78 × 0.12 | 1.9 × 12.5 × 1.9 | なし |
| 左角 | 頭 | 2104 | 付け根 | 0.12 × 0.78 × 0.12 | 1.9 × 12.5 × 1.9 | なし |
| 頭飾り | 頭 | 2105 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | なし |
| 右肩 | 人胴 | 2106 | 中心 | 0.65 × 0.48 × 0.62 | 10.4 × 7.7 × 9.9 | 立方体 0.65 × 1 |
| 左肩 | 人胴 | 2107 | 中心 | 0.65 × 0.48 × 0.62 | 10.4 × 7.7 × 9.9 | 立方体 0.65 × 1 |
| 右腕 | 人胴 | 2108 | 付け根 | 0.42 × 1.10 × 0.42 | 6.7 × 17.6 × 6.7 | 立方体 1.10 × 1 |
| 左腕 | 人胴 | 2109 | 付け根 | 0.42 × 1.10 × 0.42 | 6.7 × 17.6 × 6.7 | 立方体 1.10 × 1 |
| **槍** | 右腕 | 2110 | 付け根 | 0.28 × 3.80 × 0.28 | **1/4で描く**（1.1 × 15.2 × 1.1） | 立方体 0.76 × 5 |
| 穂先 | 槍 | 2111 | 中心 | 0.75 × 0.75 × 0.75 | 12.0 × 12.0 × 12.0 | なし |
| 馬胴 | 人胴 | 2112 | 中心 | 1.15 × 0.90 × 2.60 | 18.4 × 14.4 × 41.6 | 立方体 1.30 × 2 |
| 右前足 | 馬胴 | 2113 | 付け根 | 0.34 × 1.50 × 0.34 | 5.4 × 24.0 × 5.4 | 立方体 1.50 × 1 |
| 左前足 | 馬胴 | 2114 | 付け根 | 0.34 × 1.50 × 0.34 | 5.4 × 24.0 × 5.4 | 立方体 1.50 × 1 |
| **右後足** | 馬胴 | 2115 | 付け根 | 0.41 × 1.50 × 0.41 | 6.5 × 24.0 × 6.5 | 立方体 1.50 × 1 |
| **左後足** | 馬胴 | 2116 | 付け根 | 0.41 × 1.50 × 0.41 | 6.5 × 24.0 × 6.5 | 立方体 1.50 × 1 |

- **後足は前足の1.2倍の太さ**である。前後で使い回せない
- 馬胴は**前後（Z）に 2.60 ブロック**と長い。奥行きが最長辺である
- **頭は両形態で同寸**である。角と頭飾りだけが少し大きくなる

---

## 5. 寸法は「見た目の枠」であって「守るべき箱」ではない

表の寸法は**当たり判定の元になる大きさ**であり、モデルがこれをはみ出してはいけないという意味ではない。装飾のスパイクや布は枠の外へ出してよい。

ただし**大きくはみ出すと、見えているのに当たらない部分が生まれる**。判定は表の立方体でしか取れないため、枠から遠い装飾は「見た目だけ」として扱われる。

---

## 6. モデルの原点の較正

**ItemDisplay がモデルのどこを原点として描くかは、実機で1度だけ確かめる必要がある。** 本書の想定は「モデル座標 (8, 8, 8) が原点に来る」だが、これは検証していない。

較正の手順:

1. `頭`（ID 2002）に、**16×16×16 の立方体**（`from: [0,0,0]`, `to: [16,16,16]`）だけのモデルを割り当てる
2. `KnightDefinition.AUTHORED_MODELS` を `true` にしてビルドし、`/raid spawn`
3. 立方体が**頭の位置を中心に**出れば想定どおり。半ブロックずれていれば、全モデルを 8 単位ずらす

この1回で全部位に効く。**先に較正してから本番のモデルを描くこと。**

---

## 7. 配置例

`assets/minecraft/models/item/paper.json` に、部位ごとのモデルを紐づける。

```json
{
  "parent": "item/generated",
  "textures": { "layer0": "item/paper" },
  "overrides": [
    { "predicate": { "custom_model_data": 2001 }, "model": "knight/torso" },
    { "predicate": { "custom_model_data": 2002 }, "model": "knight/head" },
    { "predicate": { "custom_model_data": 2012 }, "model": "knight/spear" }
  ]
}
```

モデル本体は `assets/minecraft/models/knight/*.json` に置く。

---

## 8. 切り替え

モデルが揃ったら、`KnightDefinition` の1行を変える。

```java
public static final boolean AUTHORED_MODELS = true;
```

これで描画側は**バニラの素材を引き伸ばす方式をやめ、描いたモデルを等倍で出す**ようになる。寸法の宣言は当たり判定にだけ使われる。

**バニラ素材の指定は消さない。** モデルの不具合を切り分けるとき、1行戻すだけで比較できる状態を残しておく。

---

## 9. 未整備の点

| 項目 | 内容 |
|---|---|
| 槍の円錐 | バニラ素材のときは輪切り4枚で円錐を作っている。描いたモデルでは**モデル側に円錐を描く**（輪切りは使わない） |
| テクスチャの解像度 | 1ブロック=16単位に対し、テクスチャの解像度は自由。16×16でも32×32でもよい |
| 発光 | 弱点の露出はプラグイン側が発光色で示す。モデル側で光らせる必要はない |
