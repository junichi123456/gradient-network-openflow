# レイド個体のモデル寸法規定

リソースパックで部位のモデルを描くための規定。数値は `core` の骨格定義（`KnightDefinition`）から生成しており、コードを変えれば本書も更新する。

> **本書は「描く側」の規定である。** 配置・回転・当たり判定はプラグインが持つ。描く側が守るのは**原点と寸法**の2つだけである。

---

## 1. 座標系

| 軸 | 向き |
|---|---|
| +Z | 個体の**正面** |
| +Y | 上 |
| +X | 個体から見て**左** |

**「右」の部位は x が負である。** Minecraft では正面が +Z のとき、その実体から見た右は −X になる。骨格の x 座標もこれに合わせてある（`右腕` は x = −0.52）。

> モーションの角度規定（§12.6 の「正面から見て真右を0度」）は**見る側から**の表記であり、こちらとは別の話である。混同しないこと。

---

## 2. 原点（ピボット）

部位には2種類の原点がある。**モデルはこの点を中心に回る。**

| 種別 | 原点の位置 | 該当する部位 |
|---|---|---|
| **中心** | 直方体の中心 | 胴・人胴・頭・肩・馬胴・頭飾り・穂先 |
| **付け根** | 上端の面の中心。ここから**下へ**伸びる | 腕・足・角・槍 |

付け根合わせは、肩から腕が垂れる・股から足が伸びる、という回り方を作るためである。腕を振れば付け根を軸に回る。

---

## 3. 縮尺

**1ブロック = 16単位。** モデルの `elements` はこの単位で書く。

**モデルの座標は −16〜32 の範囲しか取れない。** これを超える部位は縮めて描き、プラグイン側で等倍に戻す。

> **判定は長辺ではなく「占有範囲」で行う。** 原点は (8,8,8) にあるため、付け根合わせの部位は
> そこから下へ伸びる。**長さが 1.5 ブロックを超えると下限（−16）を割る**。
> 第二形態の前足がちょうど 1.50 で、余裕がない。伸ばすなら縮尺の分母を上げること。

| 部位 | 描く縮尺 | 理由 |
|---|---|---|
| 槍以外のすべて | **1:1** | 最長辺が3ブロックに収まる |
| **槍** | **1/4** | 3.40〜3.80ブロックあり範囲を超える。プラグインが4倍に戻す |

- 戻すのは**等倍**である。テクスチャは歪まない
- 槍を1/4で描くと、第一形態で **3.0 × 13.6 × 3.0 単位**になる

---

## 4. 寸法表

`ID` はモデル識別子（`custom_model_data`）。`判定` は当たり判定の立方体の一辺と個数で、**描く側は意識しなくてよい**（参考値）。

### 第一形態（全長 3.5 / 幅 1.6）

| 部位 | 親 | ID | 原点 | 寸法（ブロック） | 寸法（単位） | 判定 |
|---|---|---|---|---|---|---|
| 胴 | — | 2001 | 中心 | 0.95 × 1.20 × 0.62 | 15.2 × 19.2 × 9.9 | 立方体 1.20 × 1 |
| 頭 | 胴 | 2002 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | 立方体 0.68 × 1 |
| 右角 | 頭 | 2003 | 付け根 | 0.11 × 0.72 × 0.11 | 1.8 × 11.5 × 1.8 | なし |
| 左角 | 頭 | 2004 | 付け根 | 0.11 × 0.72 × 0.11 | 1.8 × 11.5 × 1.8 | なし |
| 頭飾り | 頭 | 2005 | 中心 | 0.60 × 0.60 × 0.60 | 9.6 × 9.6 × 9.6 | なし |
| 右肩 | 胴 | 2006 | 中心 | 0.55 × 0.42 × 0.60 | 8.8 × 6.7 × 9.6 | 立方体 0.60 × 1 |
| 左肩 | 胴 | 2007 | 中心 | 0.55 × 0.42 × 0.60 | 8.8 × 6.7 × 9.6 | 立方体 0.60 × 1 |
| 右腕 | 胴 | 2008 | 付け根 | 0.40 × 1.10 × 0.40 | 6.4 × 17.6 × 6.4 | 立方体 1.10 × 1 |
| 左腕 | 胴 | 2009 | 付け根 | 0.40 × 1.10 × 0.40 | 6.4 × 17.6 × 6.4 | 立方体 1.10 × 1 |
| 右足 | 胴 | 2010 | 付け根 | 0.44 × 1.20 × 0.44 | 7.0 × 19.2 × 7.0 | 立方体 1.20 × 1 |
| 左足 | 胴 | 2011 | 付け根 | 0.44 × 1.20 × 0.44 | 7.0 × 19.2 × 7.0 | 立方体 1.20 × 1 |
| **槍** | 右腕 | 2012 | 付け根 | 0.75 × 3.40 × 0.75 | **1/4で描く**（3.0 × 13.6 × 3.0） | 立方体 0.75 × 5 |
| 穂先 | 槍 | 2013 | 中心 | 0.35 × 0.35 × 0.35 | 5.6 × 5.6 × 5.6 | なし |

### 第二形態（全長 4.6 / 幅 2.0）

| 部位 | 親 | ID | 原点 | 寸法（ブロック） | 寸法（単位） | 判定 |
|---|---|---|---|---|---|---|
| 人胴 | — | 2101 | 中心 | 1.00 × 1.15 × 0.62 | 16.0 × 18.4 × 9.9 | 立方体 1.15 × 1 |
| 頭 | 人胴 | 2102 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | 立方体 0.68 × 1 |
| 右角 | 頭 | 2103 | 付け根 | 0.12 × 0.78 × 0.12 | 1.9 × 12.5 × 1.9 | なし |
| 左角 | 頭 | 2104 | 付け根 | 0.12 × 0.78 × 0.12 | 1.9 × 12.5 × 1.9 | なし |
| 頭飾り | 頭 | 2105 | 中心 | 0.68 × 0.68 × 0.68 | 10.9 × 10.9 × 10.9 | なし |
| 右肩 | 人胴 | 2106 | 中心 | 0.65 × 0.48 × 0.62 | 10.4 × 7.7 × 9.9 | 立方体 0.65 × 1 |
| 左肩 | 人胴 | 2107 | 中心 | 0.65 × 0.48 × 0.62 | 10.4 × 7.7 × 9.9 | 立方体 0.65 × 1 |
| 右腕 | 人胴 | 2108 | 付け根 | 0.42 × 1.10 × 0.42 | 6.7 × 17.6 × 6.7 | 立方体 1.10 × 1 |
| 左腕 | 人胴 | 2109 | 付け根 | 0.42 × 1.10 × 0.42 | 6.7 × 17.6 × 6.7 | 立方体 1.10 × 1 |
| **槍** | 右腕 | 2110 | 付け根 | 0.75 × 3.80 × 0.75 | **1/4で描く**（3.0 × 15.2 × 3.0） | 立方体 0.76 × 5 |
| 穂先 | 槍 | 2111 | 中心 | 0.38 × 0.38 × 0.38 | 6.1 × 6.1 × 6.1 | なし |
| 馬胴 | 人胴 | 2112 | 中心 | 1.15 × 0.90 × 2.60 | 18.4 × 14.4 × 41.6 | 立方体 1.30 × 2 |
| 右前足 | 馬胴 | 2113 | 付け根 | 0.34 × 1.50 × 0.34 | 5.4 × 24.0 × 5.4 | 立方体 1.50 × 1 |
| 左前足 | 馬胴 | 2114 | 付け根 | 0.34 × 1.50 × 0.34 | 5.4 × 24.0 × 5.4 | 立方体 1.50 × 1 |
| **右後足** | 馬胴 | 2115 | 付け根 | 0.41 × 1.50 × 0.41 | 6.5 × 24.0 × 6.5 | 立方体 1.50 × 1 |
| **左後足** | 馬胴 | 2116 | 付け根 | 0.41 × 1.50 × 0.41 | 6.5 × 24.0 × 6.5 | 立方体 1.50 × 1 |

- **後足は前足の1.2倍の太さ**である。前後で使い回せない
- 馬胴は**前後（Z）に 2.60 ブロック**と長い。奥行きが最長辺である
- **頭は両形態で同寸**である。角と頭飾りだけが少し大きくなる

---

## 5. 槍の形

槍は**手元が太く先端が細い四角錐**である。長さの軸を中心に、断面が正方形のまま絞られる。

| 位置 | 断面 |
|---|---|
| 手元 | **12 ピクセル**（0.75 ブロック） |
| 先端 | **2 ピクセル**（0.125 ブロック） |

バニラの素材で示すあいだは、これを**6段の輪切り**で表す。断面は 12・10・8・6・4・2 と等間隔に刻まれ、**段の境目で面どうしが接する**（隙間はない）。

| 段 | 断面 | 第一形態の位置（手元から） |
|---|---|---|
| 1 | 12px | 0.000 〜 0.567 |
| 2 | 10px | 0.567 〜 1.133 |
| 3 | 8px | 1.133 〜 1.700 |
| 4 | 6px | 1.700 〜 2.267 |
| 5 | 4px | 2.267 〜 2.833 |
| 6 | 2px | 2.833 〜 3.400 |

**モデルを描く場合は、輪切りにせず滑らかな四角錐を1つのモデルとして描く。** 段差はバニラの素材で示すための妥協であり、規定ではない。

> **柄と角にはフルキューブの素材しか使えない。** エンドロッドのように 1×1×1 を埋めないブロックを拡大すると、モデルの隙間もそのまま拡大され、飛び飛びの箱に見える。現在は `SMOOTH_QUARTZ` を使っている。

---

## 6. 寸法は「見た目の枠」であって「守るべき箱」ではない

表の寸法は**当たり判定の元になる大きさ**であり、モデルがこれをはみ出してはいけないという意味ではない。装飾のスパイクや布は枠の外へ出してよい。

ただし**大きくはみ出すと、見えているのに当たらない部分が生まれる**。判定は表の立方体でしか取れないため、枠から遠い装飾は「見た目だけ」として扱われる。

---

## 7. モデルの原点の較正

**ItemDisplay がモデルのどこを原点として描くかは、実機で1度だけ確かめる必要がある。** 本書の想定は「モデル座標 (8, 8, 8) がエンティティの位置に来る」だが、これは検証していない。ここがずれていると、描いたモデルが**全部位まとめてずれる**。

**先に較正してから本番のモデルを描くこと。** 器材は用意してある。

### 手順

1. リソースパックを入れる（`resourcepack/README.md`）。較正用の立方体だけが入っている
2. サーバーに入って `/raid calibrate`

出るもの:

| 表示 | 意味 |
|---|---|
| **色付きの 16 単位の立方体** | 較正用のモデル（`custom_model_data 9000`） |
| **赤い小さな立方体**（一辺 0.1） | **エンティティの位置そのもの** |
| **マゼンタの角**（3 単位） | モデル座標 **(0, 0, 0)** |

面の色で向きが読める。**上=黄緑 / 下=赤 / 北(−Z)=青 / 南(+Z)=黄 / 西(−X)=白 / 東(+X)=黒**。

`/raid calibrate` は立方体を**2つ**出す。足元の位置に**補正なし**（ItemDisplay の生の挙動）、
2ブロック東（+X）に**補正あり**（骨格の表示と同じ回転を掛けたもの）である。

`/raid despawn` で消える。

### 較正の結果（**実機で確認済み**）

| 項目 | 結果 |
|---|---|
| **原点** | **モデル座標 (8, 8, 8)**。想定どおり。赤い印が立方体の中心に来た。マゼンタの角は下側にあった |
| **向き** | **ItemDisplay はモデルを Y軸まわりに180度回して描く**。補正なしの立方体は 北=黄(=モデルの南) / 南=青(=モデルの北) / 西=黒(=モデルの東) / 東=白(=モデルの西) と、南北・東西の両方が入れ替わって見えた。上下は入れ替わっていない |

**どちらも描く側の作業には影響しない。**

- 原点が (8,8,8) なので、寸法表の中心合わせの部位は**モデルの中心を (8,8,8) に置いて描けばよい**
- 180度のずれは**描画側（`BossRig`）で戻している**。描く側は **+Z を正面**として描いてよい
- **補正は描いたモデルにだけ掛かる。** バニラのアイテムを見た目に使う部位（穂先・頭飾り）は
  平面のスプライトであり、基準回転を実機の見え方に合わせて決めてある。そちらへ掛けると
  向きが逆になる（一度そうなった）
- **Z軸まわりに −45 度の傾きも掛けている**（`BossRig.MODEL_TILT_DEGREES`）。実機で見て
  決めた対症の値であり、**根本の原因は分かっていない**。座標系の取り違えなら 90 や 180 に
  なるはずで、45 という量はそれに当てはまらない。原因が分かった時点で 0 に戻す。
  描く側は**傾きを考えずに、まっすぐ描いてよい**
- `BlockDisplay` にはこのずれが無い。両者を混ぜて使うため、描画側で揃えないと前後左右が食い違う

> **なぜ ItemDisplay だけずれるのか。** Minecraft のアイテム描画は、もともと手に持った
> 向き（プレイヤーから見て正面）を基準にしている。エンティティとして出すときもその基準が
> 残るため、ブロックの描画と180度食い違う。仕様であり、直る見込みのある不具合ではない。

---

## 8. 雛形の生成

**モデルは手で書き起こさない。骨格データから生成する。** 寸法表を写し間違える余地をなくし、当たり判定と見た目の寸法が必ず一致するようにするためである。

```sh
./core/generate-pack.sh          # resourcepack/ に書き出す
```

生成されるもの:

| 場所 | 内容 |
|---|---|
| `assets/minecraft/models/knight/p1/*.json` | 第一形態の部位（13件） |
| `assets/minecraft/models/knight/p2/*.json` | 第二形態の部位（16件） |
| `assets/minecraft/items/paper.json` | `custom_model_data` からの振り分け（**1.21.4 の書き方**） |

振り分けの形は次のとおり。`overrides` / `predicate` は 1.21.4 で削除されている。

```json
{
  "model": {
    "type": "minecraft:range_dispatch",
    "property": "minecraft:custom_model_data",
    "index": 0,
    "fallback": { "type": "minecraft:model", "model": "minecraft:item/paper" },
    "entries": [
      { "threshold": 2001, "model": { "type": "minecraft:model", "model": "knight/p1/torso" } }
    ]
  }
}
```

- `entries` は **threshold の昇順**（生成側が並べる）
- `index: 0` は `custom_model_data` の数値リストの何番目を見るか。プラグインは 0 番へ書き込む

### 描き始める

生成された JSON は **Blockbench でそのまま開ける**。形も UV もそこで直せる。

テクスチャは最初バニラのブロックを指している。差し替えるときは、モデルの `"skin"` の1行を自分のテクスチャのパスへ変えるだけでよい。

```json
"textures": { "skin": "knight/torso", "particle": "#skin" }
```

`assets/minecraft/textures/knight/torso.png` を置けば効く。

> **生成し直すと手で描いた形は失われる。** 骨格の寸法を変えたときだけ生成し直し、
> 差分を見て必要な部位を描き直すこと。

---

## 9. 切り替え

モデルが揃ったら、`KnightDefinition` の1行を変える。

```java
public static final boolean AUTHORED_MODELS = true;
```

これで描画側は**バニラの素材を引き伸ばす方式をやめ、描いたモデルを等倍で出す**ようになる。寸法の宣言は当たり判定にだけ使われる。

**バニラ素材の指定は消さない。** モデルの不具合を切り分けるとき、1行戻すだけで比較できる状態を残しておく。

---

## 10. 未整備の点

| 項目 | 内容 |
|---|---|
| 槍の円錐 | Minecraft のモデルは軸に沿った箱しか持てず、真の角錐は描けない。生成側は**8段の箱で近似**している。段を増やせば滑らかになる。**1つのモデルの中で完結する**ため、バニラ素材のときと違い表示実体は増えない（6→1に減る） |
| テクスチャの解像度 | 1ブロック=16単位に対し、テクスチャの解像度は自由。16×16でも32×32でもよい |
| 発光 | 弱点の露出はプラグイン側が発光色で示す。モデル側で光らせる必要はない |
